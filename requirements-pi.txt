# Pi-only deps. On the Raspberry Pi, after requirements.txt:
#   ./.venv/bin/pip install -r requirements-pi.txt
# This pulls gpiod / spidev / numpy via the inky package and will fail on macOS.
#
# Pin >=2.4.0: the Spectra-6 (E673) driver landed in 2.1.0, but 2.2.0 fixed
# swapped green/blue colour constants and 2.2.1 fixed prepared-image dithering.
# palette.json is extracted from this line, so the version must match it
# (regenerate with scripts/extract_palette.py if you bump inky).
-r requirements.txt
inky>=2.4.0
